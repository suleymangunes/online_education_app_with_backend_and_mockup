import 'package:get/get.dart';

class SizedboxConstans {
  static SizedboxConstans instance = SizedboxConstans._init();
  SizedboxConstans._init();

  double spaceNormal = Get.height * 0.15;
  double spaceBottom = Get.height * 0.06;
  double spaceSmallBit = Get.height * 0.03;
  double spaceSmall = Get.height * 0.02;
  double textFieldNormal = Get.width * 0.8;
  double riveHeight = Get.height * 0.065;
  double riveWidth = Get.width * 0.15;
}
